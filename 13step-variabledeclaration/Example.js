"use strict";
if (true) {
    let z = "string";
}
else {
    let z = 5;
}
console.log(z); // this is an example of block scope.
const a = [1, 2, 3, 4, 5, 6, 7, 8];
a = [56, 67]; // This is not assign to it , it creates an error . 
// using method
a.push(5, 1);
